import React from 'react';

function OrderHeaderTitle(props) {
    return (
        <header>
            <h2>{props.title}</h2>
            <hr/>
        </header>
    );
}

export default OrderHeaderTitle;
